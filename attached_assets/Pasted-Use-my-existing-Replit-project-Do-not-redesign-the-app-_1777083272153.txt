Use my existing Replit project. Do not redesign the app. Keep current UI/theme unless required. Fix only the issues below carefully and test all related flows.

## PROJECT FIX REQUEST

### 1. Invoice Tab – Import CSV / Excel Not Working

There is already a feature: **Import invoices from CSV / Excel**.

Current error shown:

> No valid invoices found. Make sure the file has 'Taxable Value' and 'Purchase Date' columns.

Problem:
Even when the uploaded file has valid data and those columns exist, import still fails.

### Required Fix:

* Make CSV and Excel import work properly.
* Improve column detection using flexible matching:

  * Taxable Value
  * TaxableValue
  * Taxable value
  * taxable_value
  * Purchase Date
  * PurchaseDate
  * Invoice Date
  * Date
* Ignore capitalization, spaces, underscores.
* Parse Excel (.xlsx) and CSV correctly.
* Show imported row count after success.
* If some rows invalid, import valid rows and show skipped count.
* Do not reject full file unnecessarily.

---

### 1(B). Invoice Entry Details Text Change

Current text:

> Block credit u/s 17(5)
> Mark this if the ITC is blocked under Section 17(5) — e.g. motor vehicles, food & beverages, personal-use items. The full ITC will be shown as ineligible and Rule 43 will not be applied.

### Required Change:

Remove only this phrase:

> and Rule 43 will not be applied.

Keep rest same.

---

## 2. Turnover Tab – Filter Reset Issue

Current issue:
Filter works properly.

But when user:

* goes to another tab (Invoice etc.)
* comes back to Turnover tab

Then selected filter resets.

### Required Fix:

Persist user filter selection when changing tabs.

Use local state / localStorage / route memory so user returns with same selected filter until manually changed.

---

## 3. Reports Tab → Rule 43 Consolidated Report

When user exports report from:

**Reports > Rule 43 Consolidated**

### Required Changes:

#### A. Reversal amount should show head-wise:

* IGST Reversal
* CGST Reversal
* SGST Reversal
* Total Reversal

#### B. Remove this item only:

* Net ITC claimed

#### C. Card name:

Keep this card:

> Reversal breakdown (GSTR-3B Table 4(B)(1))

Do NOT remove other currently useful elements inside that card unless directly related to Net ITC claimed.

---

## 4. Rule 43 – Per Invoice Report

Current report mostly perfect.

### Required Fixes:

#### A. Add final total row

At bottom add totals for relevant numeric columns.

#### B. Block Credit u/s 17(5) Logic Wrong

If invoice is marked as Block Credit, report should show:

* Full credit reversal / ineligible under Section 17(5)

Currently it shows:

* ITC Retained

This is wrong.

### Correct Behaviour:

Blocked credit invoice should clearly reflect full disallowance under Section 17(5), not retained ITC.

---

## 5. General Instructions

* Keep all existing formulas unless fix is required.
* Do not break current working features.
* Test Invoice import, Turnover filter persistence, Consolidated report, Per Invoice report.
* Maintain current UI design.
* Return clean production-ready code changes only.
